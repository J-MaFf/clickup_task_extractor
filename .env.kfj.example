# Configuration for the standalone weekly sync: kfj_task_extractor.py
#
# Copy this file to `.env.kfj` and fill in your own values. `.env.kfj` is
# gitignored and loaded automatically at startup (real environment variables and
# CLI flags take precedence over it), so a plain `python kfj_task_extractor.py`
# picks up the KFJ_* settings below. You can still inject them via 1Password
# instead, which also resolves the op:// secret references:
#
#     op run --account my.1password.com --env-file=.env.kfj -- \
#         python kfj_task_extractor.py
#
# Secret handling: the auto-loader deliberately does NOT load an op:// value for
# CLICKUP_API_KEY / GOOGLE_SHEETS_CREDENTIALS_JSON (it would set the literal
# "op://..." string as the key). Those op:// references are resolved at runtime
# by `op run` or the script's 1Password chain instead. A literal key value (no
# op://) is loaded normally, and the KFJ_*_SECRET_REFERENCE pointers below are
# always loaded (their values are meant to be op:// URIs the resolver consumes).
#
# Every variable is optional. The list/sheet ids can also be passed on the CLI
# (--list-id / --sheet-id), which take precedence over these values.

# --- What to sync -----------------------------------------------------------
# ClickUp list ID to extract open tasks from. Find it in the ClickUp list URL.
# Required (via this var or --list-id).
KFJ_CLICKUP_LIST_ID=

# Google Sheets workbook (spreadsheet) ID to write the weekly tab into. This is
# the long ID in the sheet URL: https://docs.google.com/spreadsheets/d/<ID>/edit
# Required unless you run with --dry-run.
KFJ_GOOGLE_SHEET_ID=

# Optional display tweaks:
# Prefix for the dated worksheet tab (default: "KFI Jefferson current tasks").
# KFJ_TAB_PREFIX=KFI Jefferson current tasks
# Branch label used when a task has no "Branch" custom field value.
# KFJ_FALLBACK_BRANCH=

# --- ETA calculation ---------------------------------------------------------
# Tasks without a ClickUp due date get a calculated ETA. By default the local
# `claude` CLI (Claude Code OAuth/Max subscription, no API key) estimates it;
# when the CLI is missing, logged out, or usage-limited, a deterministic
# priority/status baseline is used instead. Set to 0 to skip the AI pass and
# always use the deterministic baseline (CLI equivalent: --no-ai-eta).
# KFJ_AI_ETA=1
# Bounded worker count for the AI ETA pass (same knob as the main extractor).
# AI_SUMMARY_CONCURRENCY=4

# --- Credentials ------------------------------------------------------------
# Provide the secrets directly (highest precedence). These are read by the
# resolver before any 1Password lookup is attempted.
CLICKUP_API_KEY=
# The Google service-account JSON, as a single-line string.
GOOGLE_SHEETS_CREDENTIALS_JSON=

# --- 1Password (optional) ---------------------------------------------------
# op:// references for the two secrets. When set, the script resolves the
# credentials from 1Password (desktop SDK -> fallback chain -> op CLI). When
# empty, the 1Password lookup is skipped and the variables above are used.
# References may use vault/item names or IDs, e.g. op://<vault>/<item>/credential
KFJ_CLICKUP_SECRET_REFERENCE=
KFJ_GOOGLE_SA_SECRET_REFERENCE=

# 1Password account selectors. KFJ_OP_ACCOUNT_NAME is the account *display
# name* shown in the 1Password desktop app (used by the SDK DesktopAuth);
# KFJ_OP_ACCOUNT_URL is the account URL used by the `op` CLI.
KFJ_OP_ACCOUNT_NAME=
KFJ_OP_ACCOUNT_URL=my.1password.com

# --- Unattended / headless runs (optional) -----------------------------------
# For a scheduled task that must run while the machine is locked or logged out
# ("Run whether user is logged on or not"), set OP_SERVICE_ACCOUNT_TOKEN — a
# 1Password *service account* token with read access to the vault holding the
# two secrets above. When it is set, the script resolves the op:// references
# headlessly through the 1Password SDK, before (and instead of) the desktop-app
# DesktopAuth path; when unset, nothing changes.
#
# DO NOT put the token in this file or in .env.kfj — it is secret material and
# must never live on disk in the repo. Instead set it as a user-scoped
# environment variable for the Windows account the scheduled task runs as:
#
#     setx OP_SERVICE_ACCOUNT_TOKEN "ops_..."     (run as the task's user)
#
# (Stored in that user's registry profile, picked up by Task Scheduler; do not
# pass it on the task's command line, which is visible in the task definition.)
# OP_SERVICE_ACCOUNT_TOKEN=   <- set via the OS, never here
